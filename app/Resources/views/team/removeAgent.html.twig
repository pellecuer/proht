{% extends 'base.html.twig' %}

{% block stylesheets %}
{% endblock %}
{% block title %}Welcome!{% endblock %}

{% block body %}   
    <div class="container">
        <h1>{{title}} un agent dans l'équipe : {{team.name }}</h1>
        <form action="{{ path('removeAgent', { 'id': team.id }) }}" method="post" class="form-inline">
            <div class="form-group mb-2">
            {{ form_start(form) }}
            {{ form_rest(form) }}
            {{ form_end(form) }}  
            </div
        </form>
    </div> 
{% endblock %}


{% block javascripts %}

{% endblock %}
    

